# 真實模型介面整合驗證

2026-09-10，以 `gpt-4.1-mini-2025-04-14`、temperature 0、最多 500 output tokens，
透過 FastAPI 實際訊息與確認端點，操作獨立合成 SQLite 資料庫。

| 流程 | 策略 | 資料庫驗證 |
|---|---|---|
| 冷氣維修，明天 10:00 | fixed | 確認後一筆有效預約，版本 1，日期為台北明天 |
| 改成後天 14:00 | fixed | 同筆預約，版本 2，日期為台北後天 |
| 取消我的預約 | fixed | 同筆預約 cancelled，版本 3 |
| 查詢含取消預約 | agent | 讀取後回覆，資料庫保持 cancelled、版本 3 |

四個流程通過；使用 5 次 API，新增 token 計算費用 **USD 0.0016772**。
累計含前次 pilot：38／144 次、已知費用 USD 0.0117524、保守預留 USD 0.1036804／1。
沒有未知用量。逐步回覆、工具選擇及 DB 投影見 [result.json](result.json)。

這是整合 smoke，確認操作由測試使用者明確送出；不是獨立能力評估或策略排名。
原始首輪比較報告保持不變。本次 live 提示另外提供可信台北目前時間，讓相對日期可解析。
API 真實驗證與瀏覽器驗證分開：瀏覽器以隔離的測試模型覆蓋模式切換、15 秒逾時、
同 ID 重試、確認及中斷；實際啟動的 live 介面另檢查桌面／手機呈現，不額外送付費訊息。
